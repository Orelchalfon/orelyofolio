export declare type templateParamsType = {
    to_email: string;
    from_name: string;
    user_email: string;
    message: string;
};
export declare type NotificationProps = {
    classNames: string;
    icon: string;
    emoji: string;
};
